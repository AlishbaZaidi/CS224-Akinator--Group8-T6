#pragma once
#include<iostream>

// Declaration of the newisInArray function
bool newisInArray(int value, const int* arr, int size);

// Declaration of the NewRandomNumGenerator function
int NewRandomNumGenerator (int range);